+++
title = "Maildir"
sort_by = "title"
template = "section.html"
page_template = "page.html"
description = "Maildir is a common way of storing email messages on a file system, where each message is stored as a separate file and each mail folder is a file system directory containing these files."
+++
